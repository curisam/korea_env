use_gpu: True
device: 0
early_stop:
  patience: 0
federate:
  mode: standalone
  client_num: 40
  sample_client_num: 5
  total_round_num: 200
  save_to: "checkpoints_1.0/tldr_choice_qwen_fusionmoe_u4.ckpt"
  ignore_weight: True
  save_freq: 20
  share_local_model: True
  online_aggr: False
  server_type: llmfusionmoeserver # MoE 서버 사용
  client_type: llmfusionmoeclient # MoE 클라이언트 사용

data:
  root: data/
  type: 'reddit-tldr-comparison-choice@llm'
  splits: [0.9,0.09,0.01]
  splitter: 'meta'
llm:
  tok_len: 1024
  accelerator:
    use: True
  chat:
    max_len: 1024
  adapter:
    use: True
    count: 4
    grouping:
      use: True
      round: 20  
    args: [ { 'adapter_package': 'peft', 'adapter_method': 'lora', 'r': 8, 'lora_alpha': 16, 'lora_dropout': 0.05, target_modules: [ "q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj" ] } ]
    ema_beta: 0.9  # EMA 계수
  grad_accum_step: 2
dataloader:
  batch_size: 2
model:
  type: 'Qwen/Qwen2-0.5B@huggingface_llm'
train:
  local_update_steps: 30
  batch_or_epoch: batch
  optimizer:
    # type: RMSprop
    # lr: 0.000001
    type: AdamW
    betas: (0.9, 0.95)
    lr: 0.00001
  scheduler:
    milestones: [100, 150]    # 100라운드, 150라운드에 감축
    gamma: 1.0            # ×1.0씩 곱해 감축
  is_enable_half: True
criterion:
  type: CrossEntropyLoss
trainer:
  type: llmfusionmoetrainer  
  choices: ['A', 'B']
eval:
  freq: 20
  split: ['val', 'test']
  metrics: ['loss', 'acc']
  best_res_update_round_wise_key: test_loss
  count_flops: False
outdir: exp/tldr/choice_qwen/gfl/fusionmoe_u4_1.0
expname: ""